import 'package:flutter/material.dart';

class Top_2 extends StatelessWidget {
  //const Top_2({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Gornja navigacija 2', style: TextStyle(fontSize: 30),),
      ),
    );
  }
}
