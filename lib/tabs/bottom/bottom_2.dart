import 'package:flutter/material.dart';

class Bottom_2 extends StatelessWidget {
  //const Bottom_2({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Donja navigacija 2', style: TextStyle(fontSize: 30),),
      ),
    );
  }
}
